CREATE TABLE "b_河川水位站" (
    status VARCHAR,
    "測站類別" VARCHAR,
    st_no VARCHAR,
    name_c VARCHAR,
    name_e VARCHAR,
    addr_c VARCHAR,
    addr_e VARCHAR,
    admicode VARCHAR,
    wr_dist_no VARCHAR,
    deptid VARCHAR,
    tm_x VARCHAR,
    tm_y DECIMAL,
    drainage_a DECIMAL,
    level0 DECIMAL,
    observe DECIMAL,
    reservoir DECIMAL,
    tri_name VARCHAR,
    tri_namee VARCHAR,
    remarks BOOLEAN,
    alert_leve DECIMAL,
    town_id DECIMAL,
    alert_le_1 DECIMAL,
    alert_le_2 BOOLEAN,
    planlevel DECIMAL,
    dike_heigh DECIMAL,
    setdate INTERVAL,
    ip VARCHAR,
    port DECIMAL,
    ip_src VARCHAR,
    tm_x97 DECIMAL,
    tm_y97 DECIMAL,
    longitude VARCHAR,
    latitude VARCHAR,
    village VARCHAR,
    iscrossriv DECIMAL,
    crossriver VARCHAR,
    reobsyy DECIMAL,
    obssrc DECIMAL,
    obsother BOOLEAN,
    usetype1 DECIMAL,
    usetype2 DECIMAL,
    usetype3 DECIMAL,
    usetype4 DECIMAL,
    usetype5 DECIMAL,
    usetype6 DECIMAL,
    ename BOOLEAN,
    wname BOOLEAN,
    specificpr BOOLEAN,
    trino1 DECIMAL,
    trino2 DECIMAL,
    trino3 BOOLEAN,
    damarea BOOLEAN,
    isdraw DECIMAL,
    isdrainage DECIMAL,
    leeff BOOLEAN,
    dieff BOOLEAN,
    disummary VARCHAR,
    sectioncha DECIMAL,
    isstraight DECIMAL,
    istide DECIMAL,
    issand DECIMAL,
    islightnin DECIMAL,
    issteal DECIMAL,
    isdisaster DECIMAL,
    isbadlayer DECIMAL,
    obsitems VARCHAR,
    obsitems_x DECIMAL,
    obsitems_y DECIMAL,
    wqobs BOOLEAN,
    equiptype DECIMAL,
    isrt DECIMAL,
    transseq1 DECIMAL,
    transseq2 DECIMAL,
    iscollecti DECIMAL,
    collection DECIMAL,
    ischeck DECIMAL,
    notcheckre DECIMAL,
    notcheckot BOOLEAN,
    transtypew DECIMAL,
    transtypec DECIMAL,
    transtypet BOOLEAN,
    obstype DECIMAL,
    normalobst DECIMAL,
    shortobsty DECIMAL,
    shortobsre BOOLEAN,
    shortobspl BOOLEAN,
    shortobsot BOOLEAN,
    shortobs_s BOOLEAN,
    pauseobsre BOOLEAN,
    isreplace BOOLEAN,
    replace_st BOOLEAN,
    pausedate BOOLEAN,
    nextobsdat BOOLEAN,
    obsremarks BOOLEAN,
    issolar DECIMAL,
    iscityelec DECIMAL,
    sun_w DECIMAL,
    sun_cnt DECIMAL,
    issunshine DECIMAL,
    iscover DECIMAL,
    sd DECIMAL,
    drivedista DECIMAL,
    walkdistan DECIMAL,
    maintaincy DECIMAL,
    iswarningl DECIMAL,
    solarremar BOOLEAN,
    main_empid DECIMAL,
    check_empi DECIMAL,
    main_emp_1 DECIMAL,
    check_em_1 DECIMAL,
    sand_empid BOOLEAN,
    rating_emp VARCHAR,
    is_flow DECIMAL,
    flow_cnt BOOLEAN,
    is_src_flo BOOLEAN
);

